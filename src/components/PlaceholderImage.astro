---
interface Props {
  label?: string;
  class?: string;
  dark?: boolean;
}

const { label = "Foto volgt", class: className = "", dark = false } = Astro.props;
---

<div
  class:list={[
    "group photo-frame relative flex items-center justify-center",
    dark
      ? "bg-gradient-to-br from-navy-800 via-navy-700 to-navy-900"
      : "bg-gradient-to-br from-navy-700 via-brand-700 to-navy-900",
    className,
  ]}
>
  <svg
    class="absolute inset-0 h-full w-full opacity-[0.15] transition-opacity duration-500 group-hover:opacity-[0.25]"
    xmlns="http://www.w3.org/2000/svg"
  >
    <defs>
      <pattern id="grid" width="28" height="28" patternUnits="userSpaceOnUse">
        <path d="M 28 0 L 0 0 0 28" fill="none" stroke="#4da3ff" stroke-width="1"></path>
      </pattern>
    </defs>
    <rect width="100%" height="100%" fill="url(#grid)"></rect>
  </svg>
  <div class="pointer-events-none absolute inset-0 flex items-center justify-center opacity-0 transition-opacity duration-500 group-hover:opacity-100">
    <div class="h-32 w-32 rounded-full bg-brand-400/25 blur-2xl"></div>
  </div>
  <div class="relative flex flex-col items-center gap-2 px-4 text-center">
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" class="h-8 w-8 text-brand-400/70 transition-transform duration-500 group-hover:scale-110 group-hover:text-brand-300">
      <rect x="3" y="4" width="18" height="13" rx="2" stroke="currentColor" stroke-width="1.5"/>
      <path d="M8 21h8M12 17v4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
      <circle cx="9" cy="10" r="1.4" stroke="currentColor" stroke-width="1.3"/>
      <path d="M5.5 14.5 9 11l2.5 2.5L16 9l2.5 2.5" stroke="currentColor" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round"/>
    </svg>
    <span class="text-xs font-medium uppercase tracking-wide text-white/50 transition-colors duration-500 group-hover:text-white/70">{label}</span>
  </div>
</div>
